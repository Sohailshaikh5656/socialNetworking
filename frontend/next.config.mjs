/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['localhost', 'randomuser.me', 'picsum.photos', "localhost:3300"],
    },
  }
  
  export default nextConfig
  